sudo apt-get update
// Enter into Root so that I don't have to use SUDO
sudo bash 
apt-get install apache2 apache2-doc apache2-utils 
// Might be asked if you would like to continue
Y
!!! The notes are saved on Raspberry PI
